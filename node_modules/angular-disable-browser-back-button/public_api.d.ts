import { ModuleWithProviders } from '@angular/core';
import { Router } from '@angular/router';
export declare class WindowRef {
    readonly nativeWindow: any;
}
export declare class BackButtonDisableModule {
    private router;
    private windowRef;
    private preserveScrollPosition;
    private window;
    private scrollX;
    private scrollY;
    constructor(router: Router, windowRef: WindowRef, preserveScrollPosition: boolean);
    static forRoot(config?: {
        preserveScrollPosition: boolean;
    }): ModuleWithProviders;
    private addPopStateEventListener;
    private scrollToThePreviousPosition;
    private getScrollCoordinates;
    private disableBackButton;
}
